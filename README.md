## Portfolio (Static)

Static single-page portfolio for **全正和**.

### Run locally

From this folder:

```bash
python3 -m http.server 5173
```

Then open `http://localhost:5173`.

### Customize

- Edit content in `index.html`
- Styling in `styles.css`
- Interactions (modal / scroll) in `main.js`

